import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: "inherit",
    button: {
      fontFamily: "inherit",
      textTransform: "unset"
    }
  }
});

export default theme;